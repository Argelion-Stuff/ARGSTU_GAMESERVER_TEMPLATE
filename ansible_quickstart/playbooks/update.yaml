---
- name: Server Update
  hosts: all
  vars:
    status: "Success"
  vars_files:
    - ../secret_vars.yaml
  tasks:
    - block:
        - name: Update
          include_role:
            name: update

        # - name: Reboot
        #   include_role:
        #     name: reboot
      rescue:
        - set_fact:
            status: "Failed"
